#include<stdio.h>
int main(){
    int n;
    printf("hello world\n");
    scanf("%d",&n)
    for(i=0;i<=;i++){
        printf("enter the value of PTR :");
        scanf("%d",&arr[i]);
        }
    int *ptr=&arr[];
    for(i=0;i<sizeof(ptr);i++){
        printf("%d",ptr[i]);
    }
    // printf("ptr=%p and A=%p",&ptr,&arr);
    return 0;
}